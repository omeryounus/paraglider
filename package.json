{
  "name": "paraglider",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Aero Glide: Canyon Rush — arcade precision flight through Terrain Studio biomes",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "export:terrains": "node scripts/export-studio-glbs.mjs",
    "export:blender": "blender --background --python scripts/blender_generate_assets.py",
    "export:blender:legacy": "blender --background --python scripts/blender_paraglider_pipeline.py"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
